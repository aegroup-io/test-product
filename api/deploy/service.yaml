apiVersion: v1
kind: Service
metadata:
  name: orcha-api
  labels:
    app: orcha-api
spec:
  type: LoadBalancer
  selector:
    app: orcha-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
